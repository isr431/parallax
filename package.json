{
  "name": "parallax",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "pipeline": "tsx pipeline/index.ts",
    "test": "tsx --test pipeline/**/*.test.ts",
    "frontend": "node -e \"console.log('Frontend is introduced in Step 4; see PROGRESS.md')\"",
    "build:frontend": "node -e \"console.log('Frontend build is introduced in Step 4; see PROGRESS.md')\""
  },
  "dependencies": {
    "yaml": "^2.8.1"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  }
}
